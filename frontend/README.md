# Frontend

Angular dashboard that visualizes metrics from the backend API using ng2-charts + Chart.js.

## Development
```
cd frontend
npm install
npm start
```

The dev server runs on `http://localhost:4200` and proxies `/api` to `http://localhost:3000`.

## Tests
```
cd frontend
npm test
```

## Build
```
cd frontend
npm run build
```
